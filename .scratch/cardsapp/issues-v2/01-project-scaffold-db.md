# 01 - Project Scaffold & Database Core

**What to build:** 
- Initialize Electron + Vite + React project.
- Implement basic IPC communication.
- Setup `better-sqlite3` database in the main process.
- Create unified `Cards` schema with fields for different card types, SRS tracking (`Repetitions`, `Interval`, `EaseFactor`), `UseCount`, and associated tags.
- Implement basic CRUD IPC handlers for the database.

**Status:** done
